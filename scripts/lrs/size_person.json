{
    "default": {},
    "size_ranges": [
        {
            "min": 1,
            "max": 30,
            "config": {
                "train_batch_size": 16,
                "max_train_epochs": 110,
                "save_every_n_epochs": 10
            }
        },
        {
            "min": 31,
            "max": 80,
            "config": {
                "train_batch_size": 32,
                "max_train_epochs": 60,
                "save_every_n_epochs": 10
            }
        },
        {
            "min": 81,
            "max": 1000,
            "config": {
                "train_batch_size": 48,
                "max_train_epochs": 40,
                "save_every_n_epochs": 5
            }
        }
    ]
}