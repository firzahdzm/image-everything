{
    "default": {},
    "size_ranges": [
        {
            "min": 1,
            "max": 30,
            "config": {
                "train_batch_size": 16,
                "max_train_epochs": 100,
                "save_every_n_epochs": 20,
                "unet_lr": 1,
                "text_encoder_lr": 1
            }
        },
        {
            "min": 31,
            "max": 80,
            "config": {
                "train_batch_size": 24,
                "max_train_epochs": 80,
                "save_every_n_epochs": 10,
                "unet_lr": 1,
                "text_encoder_lr": 1
            }
        },
        {
            "min": 81,
            "max": 1000,
            "config": {
                "train_batch_size": 32,
                "max_train_epochs": 60,
                "save_every_n_epochs": 10,
                "unet_lr": 1,
                "text_encoder_lr": 1
            }
        }
    ]
}